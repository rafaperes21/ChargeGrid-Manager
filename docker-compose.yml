services:
  postgres:
    image: postgres:16-alpine
    container_name: chargegrid-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-chargegrid}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chargegrid}
      POSTGRES_DB: ${POSTGRES_DB:-chargegrid}
    ports:
      - "5432:5432"
    volumes:
      - chargegrid_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-chargegrid}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  chargegrid_postgres_data:

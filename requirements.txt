# Lista consolidada de dependencias Python do projeto (backend/ + ia/), para referencia
# rapida e para ferramentas que esperam um requirements.txt na raiz (ex.: dependency graph
# do GitHub, scanners de vulnerabilidade).
#
# Para rodar os servicos localmente, cada um tem seu proprio venv e seu proprio arquivo -
# use-os, nao este:
#   cd backend && python3.11 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   cd ia      && python3.11 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
# (ou rode ./scripts/dev.sh / .\scripts\dev.ps1, que automatiza os dois passos acima)
#
# As versoes abaixo sao identicas as fixadas em backend/requirements.txt e ia/requirements.txt
# nos pacotes compartilhados - nao ha conflito entre os dois servicos.

# --- backend/ ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic-settings==2.6.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth==2.35.0
requests==2.32.3
langchain-ollama==1.1.0

# --- ia/ ---
# prophet==1.1.6 embute um cmdstan pre-compilado (nao precisa de toolchain C++ para rodar).
# cmdstanpy>=1.3 ficou mais estrito na validacao desse cmdstan embutido e quebra o carregamento
# do backend (erro "'Prophet' object has no attribute 'stan_backend'") - fixar em 1.2.0.
# pandas 3.x quebra `Prophet.fit` (ValueError: cannot reindex on an axis with duplicate labels).
prophet==1.1.6
cmdstanpy==1.2.0
pandas<3

# --- dev/test (backend/requirements-dev.txt + ia/requirements-dev.txt) ---
pytest==8.3.3
httpx==0.27.2
ruff==0.7.4
